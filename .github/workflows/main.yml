name: "⚡ EnigMano ZeroTier ARM Deployment"

on:
  workflow_dispatch:
    inputs:
      ZEROTIER_NETWORK_ID:
        description: "Your 16-digit ZeroTier Network ID"
        required: true

jobs:
  deploy-arm-rdp:
    name: "🚀 Deploy ARM RDP via ZeroTier"
    runs-on: windows-11-arm
    timeout-minutes: 360

    steps:
      - name: 🔐 Configure Local Credentials
        shell: pwsh
        run: |
          $User = "runneradmin"
          $Pass = "EnigMano2026!"
          
          if (Get-LocalUser -Name $User -ErrorAction SilentlyContinue) {
              Write-Host "⚠️ User $User exists. Updating password..."
              $Password = ConvertTo-SecureString $Pass -AsPlainText -Force
              Set-LocalUser -Name $User -Password $Password
          } else {
              Write-Host "🆕 Creating new user $User..."
              New-LocalUser -Name $User -Password (ConvertTo-SecureString $Pass -AsPlainText -Force) -FullName "Runner Admin"
          }

          if (-not (Get-LocalGroupMember -Group "Administrators" -Member $User -ErrorAction SilentlyContinue)) {
              Add-LocalGroupMember -Group "Administrators" -Member $User
              Write-Host "➕ Added $User to Administrators."
          }

      - name: 🏗️ Install ZeroTier
        shell: pwsh
        run: |
          $url = "https://download.zerotier.com/dist/ZeroTier%20One.msi"
          $outpath = "$env:TEMP\ZeroTier.msi"
          
          Write-Host "📥 Downloading ZeroTier..."
          Invoke-WebRequest -Uri $url -OutFile $outpath
          
          Write-Host "⚙️ Installing ZeroTier..."
          # Removed /norestart to allow the driver to initialize properly if needed
          $process = Start-Process msiexec.exe -ArgumentList "/i `"$outpath`" /quiet /qn" -Wait -PassThru
          if ($process.ExitCode -ne 0) { throw "MSI Installation failed with exit code $($process.ExitCode)" }

      - name: 🌐 Connect to Virtual Network
        shell: pwsh
        run: |
          $networkId = "${{ github.event.inputs.ZEROTIER_NETWORK_ID }}"
          
          # Search for the executable in common locations (Program Files vs Program Files (x86))
          $possiblePaths = @(
              "${env:ProgramFiles}\ZeroTier\One\zerotier-one_x64.exe",
              "${env:ProgramFiles(x86)}\ZeroTier\One\zerotier-one_x64.exe"
          )
          
          $zt_path = $possiblePaths | Where-Object { Test-Path $_ } | Select-Object -First 1

          if ($zt_path) {
              Write-Host "✅ Found ZeroTier at: $zt_path"
              & $zt_path -q join $networkId
              Write-Host "📡 Sent join request for $networkId"
          } else {
              Write-Error "❌ ZeroTier executable not found in expected directories!"
              exit 1
          }

      - name: 🖥️ Configure Remote Desktop (RDP)
        shell: pwsh
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop" -ErrorAction SilentlyContinue
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1
          Write-Host "✅ RDP configuration applied."

      - name: ⏳ Maintenance Loop (Keep Alive)
        shell: pwsh
        run: |
          Write-Host "🚀 Instance is LIVE. Connect via ZeroTier IP."
          for ($i=1; $i -le 360; $i++) {
            Write-Host "Heartbeat: ($i/360 mins)"
            Start-Sleep -Seconds 60
          }
